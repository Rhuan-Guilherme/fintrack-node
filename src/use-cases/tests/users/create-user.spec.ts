import { describe, expect, test } from "vitest";

describe("Teste para a criação de un novo usuário.", () => {
  test("Deve ser possível criar um novo usuário.", () => {
    // Fase de teste, ainda falta implementar o teste corretamente.
    const user = true;

    expect(user).toBe(true);
  });
});
